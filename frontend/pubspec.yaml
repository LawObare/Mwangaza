// pubspec.yaml — Flutter project configuration.
//
// Dependencies needed:
//   flutter_map: ^6.1.0     — OpenStreetMap integration (map_screen.dart)
//   latlong2: ^0.9.0        — coordinate types for flutter_map
//   http: ^1.1.0            — HTTP client (api_service.dart)
//   provider: ^6.1.1        — state management (optional, or use setState)
//   intl: ^0.19.0           — date formatting for timestamps
//
// Dev dependencies:
//   flutter_test — for widget tests.
//
// Ensure flutter_map and latlong2 are in the dependencies section.
// Unused dependencies can be removed.
name: mwangaza_frontend